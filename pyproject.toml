[project]
name = "surfscan"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Cole", email = "cole.yakonich@gmail.com" }
]
requires-python = ">=3.11"
dependencies = []

[tool.uv.build-backend]
module-root = "."
module-name = "optionsurface"

[project.scripts]
surfscan = "optionsurface:main"

[build-system]
requires = ["uv_build>=0.12.0,<0.13.0"]
build-backend = "uv_build"

[dependency-groups]
dev = [
    "pre-commit>=4.6.2",
    "ruff>=0.16.5",
]
