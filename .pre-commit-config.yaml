repos:
  - repo: https://github.com/astral-sh/ruff-pre-commit
    rev: v0.5.0
    hooks:
      # Run the linter and automatically fix what it can
      - id: ruff
        args: [ --fix ]
      # Run the formatter
      - id: ruff-format
